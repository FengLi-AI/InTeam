# React Bits component notice

The input glow, folding text and typed placeholder are locally adapted for InTeam from interaction patterns documented by React Bits. `AeroShards.tsx` / `AeroShards.css` and `Aurora.tsx` / `Aurora.css` are vendored from the official React Bits registry components and configured for the InTeam workspace.

React Bits is distributed under the MIT License with Commons Clause. The component is used inside this application and is not redistributed as a competing component library.

Sources:

- https://reactbits.dev/backgrounds/aero-shards
- https://reactbits.dev/backgrounds/aurora
